export enum ProductCategory {
  PHONES = 'Phones',
  LAPTOPS = 'Laptops',
  SPEAKERS = 'Speakers',
  HEADPHONES = 'Headphones',
  CAMERAS = 'Cameras',
}

export enum PageType {
  PREVIOUS = 'Previous',
  NEXT = 'Next',
  DOTS = '...',
}

export enum AddUserInput {
  USER_NAME = 'userName',
  EMAIL = 'email',
  PASSWORD = 'password',
  PHONE = 'phone',
  ADRESS = 'adress',
}
